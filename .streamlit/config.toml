# Local dev: open browser and skip usage-stats prompt noise.
[browser]
gatherUsageStats = false

[server]
# Safe default for Streamlit Community Cloud. Local scripts override this.
headless = true
# Avoid Streamlit Cloud scanning transformers optional vision modules
# (which causes noisy torchvision import errors in logs).
fileWatcherType = "none"
